# 迭代快照: v0.2.0 - 2026-03-22

## 变更摘要

优化命令体系，新增 diff-ut 和 fix-ut 目录级修复，简化 DT_AGENTS.md 生成，完善端到端流程。

## 详细变更

### 新增

1. **`/diff-ut` 命令**
   - 本地变更 UT 分析（提交前检查）
   - 支持 `--base` 参数指定基准分支
   - 不传参数时分析工作区变更（未 commit）
   - 与 `/mr-ut` 区分：mr-ut 用于已提交 MR，diff-ut 用于本地变更

2. **`/task-status-dt` 命令**
   - 查看批量任务执行进度
   - 显示成功/失败/待执行数量

3. **`/fix-ut` 目录级修复**
   - 参数识别：类名/文件路径 → 单个修复，目录 → 批量修复
   - 批量修复流程：
     1. 从 DT_AGENTS.md 读取 Maven 配置
     2. 整体编译验证（mvn test-compile）
     3. 修复编译错误
     4. 创建任务队列逐个修复

4. **dtagent 代理禁止事项**
   - 严禁修复业务代码
   - 只能修改 `src/test/java/` 下的测试代码

### 修改

1. **命令重命名**
   - `/generate-single` → `/generate-dt-single`
   - `/generate-dir` → `/generate-dt-dir`
   - 统一 `-dt` 后缀

2. **`/generate-dt-dir` 参数校验**
   - `{dir}` 为必需参数，无默认值
   - 未输入目录时输出提示并终止

3. **`/init-dt` 简化**
   - 从 `.idea/workspace.xml` 提取 Maven 配置（settings/profiles/JVM参数）
   - DT_AGENTS.md 只包含必要信息：
     - 测试框架版本
     - Maven 命令（带自定义参数，可直接使用）
     - Given-When-Then 模式规范
     - 命名规范
   - 删除冗余的测试类模板和经验提取

4. **Session 查看方式**
   - 修正为 `/sessions` 命令
   - 删除错误的"右侧任务栏"描述

5. **dtagent 代理配置**
   - 禁用 task 工具，避免子任务挂起
   - 设置 `default_agent: dtagent`

### 删除

1. 删除 `docs/init-guide.md` 和 `docs/packaging.md`（内容已合并到新文档）
2. 删除 `templates/skills/*/experiences/spring-boot-test.md` 和 `diamond-mock.md`

## 受影响文件

**命令文件**:
- `templates/commands/generate-dt-single.md` (重命名)
- `templates/commands/generate-dt-dir.md` (重命名+优化)
- `templates/commands/diff-ut.md` (新增)
- `templates/commands/task-status-dt.md` (新增)
- `templates/commands/fix-ut.md` (大幅优化)
- `templates/commands/init-dt.md` (简化)
- `templates/commands/mr-ut.md` (优化)

**技能文件**:
- `templates/skills/init-dt/SKILL.md` (简化)
- `templates/skills/generate-java-ut/SKILL.md` (简化)
- `templates/skills/fix-java-ut/SKILL.md` (保留项目配置读取)

**代理文件**:
- `templates/agents/dtagent.md` (新增禁止事项)

**源码**:
- `src/commands/init.ts` (新增 Maven 配置提取，简化 DT_AGENTS.md 生成)

**插件**:
- `templates/plugins/task-manager.ts` (新增 task-create-files 工具)

**文档**:
- `docs/installation.md` (新增本地包安装方式)
- `docs/usage-scenarios.md` (更新场景说明)
- `README.md` (更新命令速查表)

## 命令对照表

| v0.1.0 | v0.2.0 | 说明 |
|--------|--------|------|
| `/generate-single` | `/generate-dt-single` | 重命名 |
| `/generate-dir` | `/generate-dt-dir` | 重命名，参数必填 |
| - | `/diff-ut` | 新增，本地变更分析 |
| - | `/task-status-dt` | 新增，查看任务进度 |
| `/fix-ut {test}` | `/fix-ut {target}` | 支持目录级修复 |

## 工作流对比

**v0.1.0**:
```
开发 → /generate-dir src/main/java → 查看右侧任务栏
```

**v0.2.0**:
```
本地开发 → /diff-ut → commit → push → MR → /mr-ut
批量修复 → /fix-ut src/test/java → 自动编译验证 → 逐个修复
```

## 测试验证

- [ ] dtagent init 生成正确的 DT_AGENTS.md
- [ ] /generate-dt-dir 不传参数时提示错误
- [ ] /diff-ut 分析本地变更
- [ ] /fix-ut 目录级修复先编译验证
- [ ] /sessions 查看任务详情

## 备注

本次迭代重点关注：
1. 命令命名规范化
2. 端到端流程完整性
3. 用户体验优化（提示清晰、参数校验）
4. 配置自动化（从 IDE 提取配置）